```
# GitHub workflow files (since ci.yml was deleted)
.github/workflows/

# Preserve existing rules if any were in the original .gitignore
# (The current .gitignore content wasn't provided, so only adding relevant ignores)
```